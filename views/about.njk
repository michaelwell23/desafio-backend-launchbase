{% extends "layout.njk"%}

{% block content %}

  <div id="wrapper">
    <img
      src="{{ about.avatar_url}}"
      alt="imagem do avatar de {{about.name}}">
    <h1>{{about.name}}</h1>
    <p>
       {{about.description}} 
    </p>
    <ul>
      <li>Tecnologias:</li>
      <li>HTML</li>
      <li>CSS</li>
      <li>Javascript</li>
      <li>Node</li>
      <li>Reactjs</li>
      <li>React Native</li>
    </ul>
  </div>

  {# <div class="links" id="links-footer">
      {% for link in about.links %}
    <a href="{{ link.url }}" target="_blank">{{ link.name }}</a>
  {% endfor %}
  </div> #}

{% endblock %}